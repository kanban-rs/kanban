---
bump: patch
---

persistence-sqlite: gate `SqliteStore::pool()` behind the `test-helpers` feature so raw connection-pool access no longer bypasses the ambient-transaction routing in release builds, and delete the four dead `command_log` audit-log functions (`append_command_batch`, `load_all_command_batches`, `truncate_command_log_after`, `shift_command_log`), which had zero callers since `SqliteBackend`'s command store delegates to the in-memory mirror instead. The `command_log` table itself is left in the schema.
