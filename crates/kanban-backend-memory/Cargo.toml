[package]
name = "kanban-backend-memory"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "InMemoryStore: an ephemeral KanbanBackend implementation holding all state in memory"

[lib]
name = "kanban_backend_memory"
path = "src/lib.rs"

[dependencies]
kanban-domain = { path = "../kanban-domain", version = "^0.8" }
kanban-backend = { path = "../kanban-backend", version = "^0.8" }
uuid.workspace = true
chrono.workspace = true

[dev-dependencies]
kanban-core = { path = "../kanban-core" }
async-trait.workspace = true
tokio = { workspace = true, features = ["full"] }
